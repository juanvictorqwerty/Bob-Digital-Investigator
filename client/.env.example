# ── Next.js Frontend ──────────────────────────
# Public URL of the backend API (used client-side)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Internal URL of the backend API (used server-side within Docker)
BACKEND_INTERNAL_URL=http://django:8000